apiVersion: v1
kind: LimitRange
metadata:
  name: c8-labs-ns-limit
  namespace: ${UNAMESPACE}
spec:
  limits:
  - type: Container
    defaultRequest:
      cpu: 200m
      memory: 128Mi
    default:
      cpu: "500m"
      memory: 1Gi
    max:
      cpu: "2"
      memory: 3Gi